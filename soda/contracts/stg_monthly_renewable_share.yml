dataset: duckdb_ds/data/renewable_energy/main/stg_monthly_renewable_share

checks:
  - row_count:

columns:
  - name: country_code
    checks:
      - missing:
  - name: country_name
    checks:
      - missing:
  - name: region
    checks:
      - missing:
  - name: period
    checks:
      - missing:
  - name: renewable_twh
    checks:
      - missing:
      - invalid:
          valid_min: 0
  - name: fossil_twh
    checks:
      - missing:
      - invalid:
          valid_min: 0
  - name: nuclear_twh
    checks:
      - missing:
      - invalid:
          valid_min: 0
  - name: total_twh
    checks:
      - missing:
      - invalid:
          valid_min: 0
  - name: renewable_share_pct
    checks:
      - missing:
      - invalid:
          valid_min: 0
          valid_max: 100
