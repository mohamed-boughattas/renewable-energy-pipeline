dataset: duckdb_ds/data/renewable_energy/main/raw_monthly_capacity

checks:
  - row_count:

columns:
  - name: country_code
    checks:
      - missing:
  - name: country_name
    checks:
      - missing:
  - name: source_series
    checks:
      - missing:
  - name: period
    checks:
      - missing:
      - invalid:
          valid_min: "2016-01"
  - name: capacity_gw
    checks:
      - missing:
      - invalid:
          valid_min: 0
  - name: capacity_w_per_capita
    checks:
      - missing:
      - invalid:
          valid_min: 0
  - name: is_aggregate_entity
    checks:
      - missing:
      - invalid:
          valid_values: [true, false]
  - name: is_aggregate_series
    checks:
      - missing:
      - invalid:
          valid_values: [true, false]
  - name: fetched_at
    checks:
      - missing:
